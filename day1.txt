const counterReducer = (state = 101, action) => {
  switch (action.type) {
    case "INCREMENT":
      return state + 1;
      case "DECREMENT":
        return state - 1;

    default:
      return state;
  }
};
export default counterReducer;


----------


export const inc=()=>{
    return {
        type: 'INCREMENT'
    }
}

export const dec=()=>{
    return {
        type: 'DECREMENT'
    }
}

------
import React from "react";
import { useDispatch, useSelector } from "react-redux";
import {} from "redux";
import { inc } from "../redux/actions/counterAction";
function CounterApp(props) {
  // consuming state from  store

  const data = useSelector((state) => state.appCounter);
  const dispatch= useDispatch()

  return (
    <div>
      <p>the counter is: {data}</p>
      <hr/>
      <button onClick={()=> dispatch(inc())}>INC</button>
    </div>
  );
}

export default CounterApp;
